* make html with just 'make html'

* You can also view it by running a web server in that directory:

# Inside docs/_build/html directory.
python -m SimpleHTTPServer
Then open your browser to http://localhost:8000.


To insert Raw HTML
.. raw:: html

    <embed>
        ...
    </embed>
